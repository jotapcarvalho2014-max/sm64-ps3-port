name: Build SM64 PS3
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    container: ghcr.io/resmo/psl1ght:master
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: |
          mkdir -p build/ps3
          cp bin/baserom.us.z64 ./baserom.us.z64
          make COMPARE=0 -j$(nproc)
      - name: Upload PKG
        uses: actions/upload-artifact@v4
        with:
          name: sm64-ps3-pkg
          path: |
            ./*.pkg
            ./build/ps3/*.pkg
